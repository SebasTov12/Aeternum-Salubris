env
# Puerto del servidor
PORT=3000

# MongoDB (local)
MONGODB_URI=mongodb://localhost:27017/aeternum-salubris

# O si usas MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/aeternum-salubris?retryWrites=true&w=majority

# JWT (secreto para tokens)
JWT_SECRET=mi_secreto_super_seguro_cambiar_en_produccion
JWT_EXPIRE=7d

# Entorno
NODE_ENV=development

# Frontend URL
FRONTEND_URL=http://localhost:5500

# Email (OPCIONAL - solo si quieres enviar emails)
EMAIL_SERVICE=gmail
EMAIL_USER=tu_email@gmail.com
EMAIL_PASS=tu_app_password_aqui